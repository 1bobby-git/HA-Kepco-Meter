## 한전ON v0.3.5

### 아파트 종합계약 및 파워플래너 개선

- 종합계약과 종합계약/나를 허용하고 기존 고객·엔티티 식별자를 유지합니다.
- 아파트 최신 청구 조회에도 파워플래너를 추가합니다. 공식 페이지의 SI_CUST_NO와 전체 계약변경일 매핑을 사용합니다.
- 현재 사용량은 유효한 F_AP_QT만 수용하며 오류 코드, null, 비정상 숫자를 구분합니다.
- PREDICT_TOT의 kWh 단위는 검증되지 않았고 공식 XML은 예상 요금으로 설명합니다. 기존 예측 사용량 ID는 보존하지만 근거 없는 에너지 수치 표시는 중단합니다.
- 부가 조회의 실패 원인을 두 센서의 속성으로 표시하고 월별 요금 조회는 유지합니다.
- 실계정의 현재 사용량 제공 여부와 예측 사용량은 이번 테스트로 보장하지 않습니다.

기존 주택용 직접계약 호환성 개선과 주택용 파서 안정성 보강을 유지합니다.
최소 Home Assistant 버전은 2026.8.3입니다.

### 이전 릴리스 기록

## 한전ON v0.3.4

2026-09-06 / Home Assistant 통합 패치

## 변경 내용

- TCP/HTTP 분할 수신을 응답 전체로 오인하는 단일 read(n) 처리 수정
- 기존 2 MiB 제한·요청 타임아웃·호스트/경로 제한·재시도·인증 실패 구분은 유지
- 로그인 준비 페이지와 JSON 응답 모두 EOF까지 크기 제한을 지켜 수신
- 이 패치는 이전 로그인 장애의 모든 원인이 해결됐다는 의미는 아니며 실제 계정 검증이 필요

## 호환성과 범위

도메인, 기존 설정, 엔티티·장치 식별자와 공개 서비스 이름은 유지합니다. 의존성 일괄 업그레이드와 서버 측 세션 만료 우회는 포함하지 않습니다. SmartThings 브리지 앱과 ANCS 펌웨어는 변경하지 않습니다.

## 검증 범위

원본 전체 파일 해시를 확인한 뒤 기존 저장소 테스트를 변경 전후 실행합니다. HACS/Hassfest와 저장소별 CI 검증을 통과한 경우에만 릴리스를 게시합니다. 앞선 후보 묶음의 오프라인 테스트 103개와 실제 저장소 테스트 결과는 별개입니다. 운영 HA의 실제 로그인·쿠키 수명·재생·실기기 제어·장시간 운용은 검증하지 않았습니다. 테스트 통과만으로 모든 환경에서 무영향이나 성능 향상을 보장하지 않습니다.

## 업데이트와 되돌리기

HA 백업을 확보한 후 HACS에서 0.3.4을 내려받고 Home Assistant를 재시작합니다. 문제가 생기면 HACS 재다운로드에서 이전 버전 0.3.3을 선택하거나 백업을 복원합니다. 이 GitHub 배포 작업은 사용자 HA에 직접 설치하거나 재시작하지 않습니다.

기준 커밋: `d7102641a45c7f968731cb07d2e6e5c3bcdcb100`

배포 검증 실행: https://github.com/1bobby-git/HA-Kepco-Meter/actions/runs/33999966164

분할 응답·빈 응답·크기 제한 경계·초과·타임아웃·취소의 회귀 테스트를 추가했습니다.

## 이전 릴리스 참고 (v0.3.3)

## 한전ON v0.3.3

### 로그인 응답 호환성 개선

- 로그인 성공 응답에서 선택 필드인 `token`이 생략되어도 `refreshToken`, `userId`, `mbrsNm`이 유효하면 세션을 생성합니다.
- `/sessionCheck` 응답에서 `token`이 생략되면 기존 세션의 값을 유지합니다.
- 인증에 필요한 필수 필드 검증과 비밀번호 오류 판정은 그대로 유지합니다.

### 오류 진단 및 버전 정합성 개선

- 응답 형식 오류를 로그인, 계정 유형, 고객번호 목록 단계로 나누어 표시합니다.
- `manifest.json`, `const.py`, `pyproject.toml`, README와 요청 User-Agent의 버전을 `0.3.3`으로 통일했습니다.
- 2026년 9월 4일 공개 프로토콜 점검에서 초기화 페이지와 잘못된 계정 로그인 응답의 기존 엔드포인트·JSON 래퍼가 유지됨을 확인했습니다. 실제 계정의 성공 응답은 사용자 자격증명 없이 검증하지 않았습니다.

- 기존 `v0.3.2`의 주택용 직접계약 호환성 개선은 그대로 유지됩니다.
- 기존 `v0.3.2`의 주택용 파서 안정성 보강도 그대로 유지됩니다.
최소 Home Assistant 버전은 2026.8.3입니다.
