## 한전ON v0.2.1

### 이름 표시 개선

- 통합 허브 제목을 기본 `한전ON` 대신 선택 세대 위치로 표시합니다. 예: `1402동 406호`.
- 동·호 값에 붙은 불필요한 앞자리 0을 표시 단계에서 제거합니다. 예: `0406호` → `406호`.
- 5개 논리 기기 이름에서 `한전ON` 접두어와 세대 위치 접미어를 제거했습니다.
  - 월별 사용량
  - 검침/전기사용량
  - 전기요금
  - 이웃 전기사용량 비교
  - 온실가스 배출량
- 각 기기 안의 엔티티는 기존 번역 센서명만 표시하며 기존 unique ID와 엔티티 연결은 유지합니다.

### 센서 정보 분리

`검침/전기사용량` 기기의 다음 3개 엔티티를 일반 `센서` 영역에서 Home Assistant의 `센서 정보` 영역으로 이동했습니다.

- 검침일
- 전기 사용 기간 시작일
- 전기 사용 기간 종료일

나머지 7개 사용량·지침 엔티티는 기존 `센서` 영역에 유지됩니다.

### Home Assistant 로컬 브랜드 이미지

- `custom_components/kepco_on/brand/`에 한전ON 기본 아이콘과 전체 로고를 포함했습니다.
- `icon.png`는 한전ON의 빨간색 O 심볼을 사용하며 256×256 투명 PNG입니다.
- `icon@2x.png`는 512×512 고해상도 투명 PNG입니다.
- `logo.png`와 `logo@2x.png`는 전체 한전ON 로고이며 배경과 불필요한 여백을 제거했습니다.
- Home Assistant 2026.3 이상에서는 로컬 Brands Proxy API가 이 이미지를 기존 Brands CDN보다 우선 사용합니다.

### 업그레이드

- 기존 v0.2.0 설정 항목은 v3으로 자동 마이그레이션되며 허브 제목이 선택 세대 위치로 갱신됩니다.
- 사용자가 설정 시 명시적으로 입력한 표시 이름은 그대로 유지합니다.
- 여러 세대를 선택한 경우 허브 제목은 `첫 세대 위치 외 N세대` 형식으로 표시합니다.
- 업데이트 후 Home Assistant Core를 완전히 재시작해야 합니다.

최소 Home Assistant 버전은 2026.8.3입니다.
