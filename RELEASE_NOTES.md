## 한전ON v0.3.10

- Home Assistant 재시작 후 한전ON 세션을 안정적으로 복원하도록 `JSESSIONID`와 `kepcoSSO` 인증 쿠키만 private Store에 제한적으로 보존합니다. 쿠키는 기존과 동일하게 한전 도메인·경로·만료 검증을 통과한 경우에만 저장·복원합니다.
- 실제 한전ON `/sessionCheck`가 `{"result": true}`만 반환하는 정상 응답을 허용하고, 저장된 refresh token·사용자 세션 정보를 그대로 유지합니다. 일부 세션 필드만 반환하는 비정상 응답은 계속 오류로 처리합니다.
- 보호 API 요청 중 세션 쿠키가 갱신되면 최신 쿠키 스냅샷을 다시 저장해 다음 Home Assistant 재시작에서도 이어서 사용합니다.
- 저장된 세션 데이터가 손상되거나 검증 응답 형식이 바뀐 경우, `비밀번호 저장`이 켜져 있으면 기존 설정을 삭제하지 않고 저장 세션을 초기화한 뒤 자동 재로그인으로 복구합니다. 비밀번호를 저장하지 않은 사용자는 실제 서버 세션이 만료된 경우에만 재인증이 필요합니다.
- HACS 업데이트 후 Home Assistant를 재시작하면 적용됩니다. 서버 측에서 세션 자체를 강제로 만료하는 경우까지 무기한 유지할 수 있는 것은 아닙니다.
